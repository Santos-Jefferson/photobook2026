Photobook is deployed.

{{- if .Values.ingress.enabled }}
URL(s):
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else }}
No Ingress is enabled. Reach it with a port-forward:

  kubectl port-forward svc/{{ include "photobook.fullname" . }} 8080:{{ .Values.service.port }}
  open http://localhost:8080
{{- end }}

Health check: GET /healthz

Note: the pods need outbound internet for the neural narration (Microsoft Edge
voices) and translation (Google). If your cluster has no direct egress, set
env.HTTPS_PROXY in values.yaml.
